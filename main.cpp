#include "Minesweeper.h"

int main()
{
	print_matrix(fill(5), 5);

	/*for (int i = 0; i < 10; i++)
	{
		for (int j = 0; j < 10; j++)
		{
			if (j % 3 == 0)
				matrix[i][j] = bomb;
			if (matrix[i][j].is_revealed)
				cout << matrix[i][j].revealed_sign;
			else
				cout << matrix[i][j].unreaveled_sign;
		}
		cout << "\n" << "\n";
	}*/

}
